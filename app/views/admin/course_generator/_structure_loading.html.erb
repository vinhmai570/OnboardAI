<!-- Structure Generation Loading State -->
<div class="mt-4 p-4 bg-blue-50 border border-blue-200 rounded-lg"
     data-controller="turbo-stream-source"
     data-turbo-stream-source-src-value="<%= turbo_stream_from "course_generator_#{course_id}" %>">
  <div class="flex items-center space-x-3">
    <div class="animate-spin rounded-full h-5 w-5 border-b-2 border-blue-600"></div>
    <div class="text-sm text-blue-800">
      <div class="font-medium">Generating Course Structure...</div>
      <div class="text-xs text-blue-600 mt-1">
        Creating course structure using AI and conversation context.
        <span class="font-mono">#<%= conversation_id %></span>
      </div>
    </div>
  </div>

  <!-- Progress indicators -->
  <div class="mt-3">
    <div class="flex items-center space-x-2 text-xs text-blue-600">
      <div class="flex items-center">
        <div class="animate-pulse bg-blue-300 rounded-full h-2 w-2 mr-1"></div>
        Analyzing conversation context
      </div>
      <div class="text-blue-300">→</div>
      <div class="flex items-center opacity-60">
        <div class="bg-blue-200 rounded-full h-2 w-2 mr-1"></div>
        Generating course modules
      </div>
      <div class="text-blue-200">→</div>
      <div class="flex items-center opacity-40">
        <div class="bg-blue-100 rounded-full h-2 w-2 mr-1"></div>
        Creating detailed structure
      </div>
    </div>
  </div>

  <!-- Auto-redirect message -->
  <div class="mt-3 p-2 bg-blue-100 rounded text-xs text-blue-700">
    💡 You'll be automatically redirected to the course structure page when generation is complete.
  </div>
</div>

<%= turbo_stream_from "course_generator_#{course_id}" %>
